packages:
  - "apps/*"
  - "packages/*"

# One pinned version per dependency, shared across every package.
# Reference in package.json as "catalog:". Bump here, not in N files.
catalog:
  # runtime
  react: ^19.0.0
  react-dom: ^19.0.0
  next: ^15.1.0
  drizzle-orm: ^0.38.0
  postgres: ^3.4.5
  # cloudflare / deploy
  "@opennextjs/cloudflare": ^1.0.0
  wrangler: ^3.99.0
  "@cloudflare/workers-types": ^4.20241205.0
  drizzle-kit: ^0.30.0
  # build / dev tooling
  typescript: ^5.7.0
  turbo: ^2.3.0
  prettier: ^3.4.0
  # linting
  eslint: ^9.17.0
  "@eslint/js": ^9.17.0
  "@eslint/eslintrc": ^3.2.0
  typescript-eslint: ^8.18.0
  eslint-config-prettier: ^9.1.0
  eslint-config-next: ^15.1.0
  globals: ^15.14.0
  # types
  "@types/node": ^22.10.0
  "@types/react": ^19.0.0
  "@types/react-dom": ^19.0.0

# Postinstall scripts are blocked by default. Allow only the native binaries we
# use: esbuild (drizzle-kit/tsx), workerd (wrangler dev/preview/deploy),
# unrs-resolver (eslint's TS import resolver). sharp isn't needed on Workers.
allowBuilds:
  esbuild: true
  workerd: true
  unrs-resolver: true
  sharp: false
