node_modules
dist
build
.turbo
coverage
*.log
.DS_Store
.env
.env.*
!.env.example

# next / opennext / wrangler
.next
.open-next
.wrangler
*.tsbuildinfo
.dev.vars
.dev.vars.*
!.dev.vars.example
# next-env.d.ts is committed (kept static) so `tsc --noEmit` runs without a prior build
