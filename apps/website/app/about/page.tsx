import { Container } from "@truelend/ui";

export const metadata = { title: "About" };

// Stub — replaced by the full page in a later step.
export default function Page() {
  return (
    <Container className="py-24">
      <h1 className="font-display text-4xl font-extrabold tracking-tight">About</h1>
      <p className="mt-4 text-navy-600">This page is being prepared.</p>
    </Container>
  );
}
