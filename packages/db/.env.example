# Used only by drizzle-kit for migrations/studio (runs from Node, not the Worker).
# The Worker itself connects through the Cloudflare Hyperdrive binding.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/truelend
